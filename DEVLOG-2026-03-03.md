# Onvord 开发日志（2026-03-03）

## 今日目标
- 解决旧版实时语音 WebSocket 握手失败的回归问题。
- 统一“录制中”和“录制后预览”的视觉与交互，降低页面切换割裂感。
- 优化语音占位提示与截图展示方式，提升可理解性。

## 主要改动

### 1) STT 握手修复
- 将旧版 STT 参数 `interim_results` 恢复为 `true`，避免握手失败回归。
- 保持前端“只展示 final 文本”的策略不变。

涉及文件：
- `sidepanel.js`

### 2) 录制页与预览页统一
- 移除独立的“大卡片 SOP 预览”页面切换。
- 停止录制后仍在同一时间线区域展示结果。
- 保留录制/暂停/完成三态，完成态仅切换底部动作按钮（导出、重录）。

涉及文件：
- `sidepanel.html`
- `sidepanel.js`
- `sidepanel.css`

### 3) 标签内缩略图 + 点击放大
- 预览标签支持内嵌小截图缩略图。
- 点击缩略图弹层放大查看。
- 增加 Esc/遮罩/关闭按钮退出预览。

涉及文件：
- `sidepanel.html`
- `sidepanel.js`
- `sidepanel.css`

### 4) 录制中也展示截图（与预览尽量一致）
- 录制中操作标签改用与预览一致的 pill 渲染样式。
- 若事件自带截图则即时显示；若截图异步生成则后补到对应标签。
- 背景脚本在截图完成后向侧边栏发送 `EVENT_SCREENSHOT` 消息。

涉及文件：
- `sidepanel.js`
- `background.js`

### 5) 语音占位文案优化
- 将未识别出文本时的占位从 `┄┄┄` 改为 `识别中`。

涉及文件：
- `sidepanel.js`

## 当前截图策略（代码现状）
- 自动截图采集：`click` / `select`。
- 前端展示策略：不按事件类型判断，凡事件携带截图字段就展示缩略图。

## 提交记录
- `5900a19` feat: unify recording and preview timeline with inline thumbnails

## 后续可选优化
- 明确并产品化“哪些事件应采集截图”的规则（例如是否扩展到 input/navigation）。
- 为录制中截图补全链路增加轻量 loading 态（避免用户误判为未捕获）。
- 对时间线中的滚动合并项，考虑使用单独视觉样式提升辨识度。
